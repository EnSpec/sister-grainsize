# DO NOT DELETE
# THIS CONFIG IS AUTO-GENERATED BY ADE UI
algo_name: sister-grainsize 
version: 0.3 
environment: ubuntu
repository_url: https://github.com/EnSpec/sister-grainsize.git
#docker_url: registry.imgspec.org/root/ade_base_images/enspec-sister:0.1
docker_url: registry.imgspec.org/root/ade_base_images/vanilla:1.0

# fill out these fields
# explain what this algorithm does
description: Snow grain size PGE for L2A_SI_TRAIT
# path to the wrapper script for running the algorithm
run_command: sister-grainsize/.imgspec/imgspec_run.sh
# set a storage value in GB or MB or KB, e.g. "100GB", "20MB", "10KB"
disk_space: 50GB
inputs:
# remove below this line if no inputs
# rename and set algorithm input names accordingly
  - name: rfl_file
    type: file
  - name: out_dir
    type: positional